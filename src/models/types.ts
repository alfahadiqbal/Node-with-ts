export enum API_TYPE {
    GET_ALL = "GET_ALL",
    CREATE = 'CREATE',
    GET = "GET",
    UPDATE = "UPDATE",
    DELETE = "DELETE",
}

export enum ROLE {
    USER = 'USER',
    MANAGER = 'MANAGER',
    ADMIN = 'ADMIN'
}